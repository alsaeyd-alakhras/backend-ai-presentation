# SouqPro — مراجعة شاملة للمحاور 2 → 14

> ملف تعليمات لأي Agent يعدّل على صفحة Notion "SouqPro - توزيع المحاضرات".
> النطاق: من **محور 2 حتى النهاية**. محور 1 اتعالج بملف منفصل (`souqpro-module1-revision.md`).
> **المبدأ الحاكم لكل التعديلات:** التنظيم حسب "مرحلة بناء المتجر الفعلية" مش حسب "تصنيف الأداة التقنية". أي موضوعين يخدموا نفس مرحلة البناء (مثلاً: حماية Admin) يجب أن يكونوا متجاورين، حتى لو كانوا تقنياً "مواضيع مختلفة" (Middleware + Breeze). وأي موضوع معقد يُؤجَّل لحد ما يكون عند الطالب أساس كافي يبني عليه.

---

## 📊 الجدول العام المحدّث (بعد التعديل)

| # | المحور | الحالة | التغيير الرئيسي |
|---|---|---|---|
| 1 | الأساسيات والإعداد | ✅ اتعالج بملف منفصل | Views قبل Controller، Service Container محذوف، انظر `souqpro-module1-revision.md` |
| 2 | تصميم قاعدة البيانات | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 3 | Eloquent والـ CRUD | 🔧 تعديل + استقبال | Controller/Route الأساسي ينتقل هنا كأول محاضرة، وBreeze البسيطة تسبقه (زي ترتيب الصفدي بالضبط) |
| 4 | العلاقات والـ Scopes | ✂️ تخفيف | 5 محاضرات → مقترح 3، دمج مواضيع متشابهة |
| 5 | الهوية والصلاحيات | 🔀 إعادة ترتيب جذرية | Breeze انتقلت بالكامل لمحور 3، هون يبقى Middleware/Roles فقط + Gates/Policies تنتقل خارج المحور بالكامل |
| 6 | ميزات المتجر الأساسية | 🔧 تعديل بالمحتوى الداخلي | استقبال Fortify/Socialite من محور 5 في مكانهم الصحيح |
| 7 | الدفع الإلكتروني | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 8 | REST API | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 9 | Real-time & Notifications | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 10 | Events & Queues | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 11 | Localization | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 12 | النظام والأداء والأمان | 🔀 إعادة تشكيل | Gates/Policies تنتقل هنا لتكون المحتوى الرئيسي، Security الأساسي يعاد تنظيمه |
| 13 | الإطلاق والنشر | ✅ جيد، بدون تغيير | لا تعديل مطلوب |
| 14 | الذكاء الاصطناعي | ✅ جيد، بدون تغيير | لا تعديل مطلوب |

---

# 🔵 محور 2 — تصميم قاعدة البيانات

**الحكم: ✅ ممتاز كما هو.**

هذا المحور من أقوى نقاط الدورة الحالية — يعلّم "التفكير قبل الكود" (ER Diagram) قبل الـ Migrations نفسها، ويبني كل الجداول الأساسية (`users`, `categories`, `products`, `carts`, `addresses`) دفعة واحدة ومنطقية.

**لا تعديل مطلوب. يبقى بمحاضرتيه الحاليتين (Migrations + Seeders) بدون أي تغيير بالمحتوى.**

---

# 🟢 محور 3 — Eloquent والـ CRUD

**الحكم: 🔧 تعديلان بالبداية فقط، الباقي ممتاز.**

### التعديل الأول — Controllers/Routes
كما اتفقنا بمراجعة محور 1: محاضرة "Routes وControllers" (كانت أصلاً محاضرة 4 بمحور 1) **تنتقل لتصبح أول محاضرة في هذا المحور**، قبل محاضرة "Eloquent Basics" الحالية.

### التعديل الثاني — Breeze تنتقل لهون من محور 5 (تصحيح أخير مهم)
**قرار محدَّث:** بعد إعادة فحص ترتيب الصفدي بدقة، Breeze عنده مش محور مستقل — هي **محاضرة واحدة بسيطة تسبق أول Resource Controller مباشرة** (محاضرة 7 عنده، قبل محاضرة 8 "CRUD: Resource Controllers"). القرار: **نتبع نفس المنطق بالضبط**.

**الفرق عن القرار السابق:** كنا خططنا Breeze تبقى بمحور 5 (بعد محور 4 Relationships). هذا **تراجع عن ذلك القرار** — Breeze البسيطة (تسجيل دخول/خروج فقط، بدون Gates/Policies) تنزل الآن **جوا محور 3، كأول محاضرة فيه (قبل حتى Controllers/Routes بالعمق)**، تماماً مثل الصفدي.

**ملاحظة توضيحية مهمة (لتفادي التناقض مع قرار سابق):** هذا لا يتعارض مع قرار "الصلاحيات لا تسبق الـ CRUD" — ذلك القرار كان خاصاً بـ **Gates/Policies** (الصلاحيات الدقيقة: مين يعدّل شو)، وهي لسا باقية متأخرة بمحور 12. Breeze نفسها مجرد تسجيل دخول بسيط بدون أي منطق صلاحيات، فلا مانع تسبق الـ CRUD.

### الترتيب الجديد للمحور (9 محاضرات بدل 7)
1. **[منقولة من محور 5] Breeze وأساسيات Authentication** — تثبيت Breeze، Login/Register/Password Reset، `auth()->user()`, `@auth`/`@guest` — بسيطة ومباشرة، بدون أي حديث عن Roles أو Gates بعد
2. **[منقولة من محور 1] Controllers & Routes بالعمق** — تذكير سريع بأساسيات Controller/Route (اتشرحت بمحاضرة 4 من محور 1)، ثم مباشرة للمتقدم: Resource Controllers والـ 7 methods، `Route::resource()`, Route Groups, Named Routes، `php artisan route:list`
3. Eloquent Basics (كما هي)
4. CRUD: Create & Read + Pagination (كما هي)
5. CRUD: Update & Delete (كما هي)
6. Validation وCustom Rules (كما هي)
7. File Upload وStorage (كما هي)
8. CRUD تطبيق كامل — SouqPro (كما هي)
9. عرض المنتجات للزوار (كما هي)

**لا حذف ولا إضافة بالمحتوى التقني لهذه المحاضرات — فقط نقل مكانها.**

---

# 🟣 محور 4 — العلاقات والـ Scopes

**الحكم: ✂️ يحتاج تخفيف حقيقي — هذا هو المحور اللي حسّيت فيه "التفاصيل كثيرة"، وهذا صحيح.**

### التشخيص
5 محاضرات لموضوع واحد فعلياً محدود (العلاقات + Scopes + Soft Deletes) مبالغ فيها. بالمقارنة، عند الصفدي نفسه هذا الموضوع أخذ **3 محاضرات فقط** (HasMany, HasOne, BelongsToMany) بدون Scopes منفصلة عنهم. السبب: بعض المحتوى الحالي عندك **تكراري أو تفصيلي زيادة عن حاجة طالب في هذه المرحلة المبكرة نسبياً**.

### أين بالضبط الزيادة؟

**محاضرة 15 (Local/Global Scopes):**
- Local Scopes (`scopeActive`, `scopeFeatured`) — **مفيدة ومهمة، تبقى**
- Global Scopes مع `withoutGlobalScope()` — **هذا مفهوم متقدم نسبياً** (تطبيق شرط تلقائي على كل query لموديل معين) ومصدر ارتباك شائع للمبتدئين (نسيان وجود Global Scope يسبب bugs غامضة). **مقترح: يتحول لإشارة مختصرة فقط ("موجود هذا المفهوم، هذا مثاله")، بدون التعمق في بناء واحد كامل من الصفر الآن.**

**محاضرة 16 (Soft Deletes):** محتوى صحيح ومباشر، **لا تخفيف مطلوب هنا** — هذا مفهوم عملي بسيط الشرح وواسع الاستخدام.

**محاضرة 18 (HasOne — User/Profile):**
- هذه أبسط علاقة أصلاً (One-to-One) ولا تحتاج محاضرة 60 دقيقة كاملة لحالها.
- **مقترح: تُدمج مع محاضرة HasMany (17) كجزء ثانٍ من نفس المحاضرة**، بدل محاضرة منفصلة. الفكرتين (`hasMany`/`belongsTo` و`hasOne`/`belongsTo`) متشابهتان جداً بالبنية، وتدريسهما معاً بمثال واحد متصل (Category → Products لـ HasMany، ثم User → Profile لـ HasOne بنفس المنطق) أوضح من الفصل.

### الترتيب المخفف المقترح (3 محاضرات بدل 5)

1. **HasMany + HasOne (مدموجتان)** — العلاقة الأولى (Category/Products) بعمق، ثم HasOne (User/Profile) كتطبيق ثانٍ أسرع لنفس المنطق. تشمل: `hasMany`, `belongsTo`, `hasOne`, N+1 Problem, Eager Loading (`with()`), `withCount()`, `firstOrCreate()`
2. **BelongsToMany — Product Tags** (كما هي، بدون تغيير — هذه العلاقة معقدة كفاية لتستحق محاضرة كاملة لحالها: Pivot Table, `attach/detach/sync/toggle`, `withPivot`)
3. **Scopes وSoft Deletes (مدموجتان)** — Local Scopes بعمق + إشارة مختصرة لـ Global Scopes + Soft Deletes كاملة (`SoftDeletes` trait, `withTrashed`, `restore`, `forceDelete`)

**النتيجة:** من 5 ساعات إلى ~3-3.5 ساعة، بدون فقدان أي مفهوم أساسي — فقط دمج المتشابه وتخفيف العمق بالمتقدم غير الضروري بهذه المرحلة (Global Scopes).

---

# 🔴 محور 5 — الهوية والصلاحيات (إعادة ترتيب جذرية)

**الحكم: 🔀 هذا أكبر تعديل بالدورة كلها. تحديث: Breeze نفسها انتقلت من هنا لمحور 3 (انظر أعلاه) — هذا المحور يتقلص أكثر مما كان مخططاً.**

### المبدأ الجديد (تم الاتفاق عليه، بعد التصحيح الأخير)
- **Breeze** (المصادقة البسيطة) → **انتقلت بالكامل لمحور 3**، قبل أول CRUD مباشرة (زي الصفدي بالضبط)
- **Fortify + Socialite** → يبقوا مرتبطين بـ **واجهة العميل (Storefront/Frontend)**، بمحور 6
- **Gates/Policies** (الأكثر تعقيداً) → تخرج بالكامل لمحور 12، بعد ما يكون عند الطالب نظام متجر شبه مكتمل. **2FA** تُؤجَّل أيضاً كملحق اختياري.

### محور 5 الجديد (يتقلص لمحاضرة واحدة فقط — "حماية لوحة التحكم بالأدوار")

1. **Middleware وRoles للأدمن** (دمج محاضرة 24 القديمة بمحاضرة 21 القديمة، بحذف كل ما يخص Fortify/Socialite/Custom Auth منها) — `AdminMiddleware` مخصص، عمود `role` (admin/customer)، حماية كل Routes الخاصة بالـ Admin (المبنية أصلاً من محور 3 بعد ما صارت محمية بـ Breeze)، `isAdmin()` helper

**ملاحظة تنظيمية:** بما إنه المحور صار محاضرة واحدة بس، فيه خياران للـ Agent المنفّذ:
- **خيار أ (مقترح):** دمج هذه المحاضرة الوحيدة داخل محور 3 كمحاضرة أخيرة فيه (مباشرة بعد Breeze في بداية المحور، أو في نهايته بعد اكتمال الـ CRUD) — يصبح محور 3 كتلة واحدة متكاملة: Auth → Controllers → CRUD → حماية بالأدوار
- **خيار ب:** يبقى محور 5 قائماً كمحور صغير بمحاضرة واحدة، فاصل بين محور 4 (Relationships) ومحور 6 (ميزات المتجر) — أبسط للتتبع الإداري بـ Notion رغم صغر حجمه

**ما يُحذف نهائياً من الدورة كمحاضرة مستقلة:**
- ❌ "مقارنة Breeze vs Fortify" — لا داعي، كل واحد لسياق مختلف
- ❌ Custom Authentication عبر `Fortify::authenticateUsing()` كموضوع منفصل — تفصيل متقدم زيادة
- ❌ Multi Guard Authentication كمحاضرة كاملة — يُشرح بالسياق العملي وقت تقديم Fortify بمحور 6

**ما ينتقل لمحور 6 (وليس محذوفاً):** Fortify، Socialite (Google Login)

**ما ينتقل لمحور 12 (وليس محذوفاً):** Gates، Policies، 2FA

---

# 🟠 محور 6 — ميزات المتجر الأساسية (يستقبل Fortify + Socialite)

**الحكم: 🔧 المحتوى الحالي ممتاز ومنطقي جداً (بناء تدريجي: Cart → Checkout → Orders → Wishlist → Reviews). التعديل الوحيد هو إضافة محاضرة الآن.**

### أين بالضبط تدخل Fortify/Socialite؟

المكان الطبيعي: **قبل محاضرة "سلة التسوق: Database" (26) مباشرة، بعد "سلة التسوق: Session" (25)**.

**السبب المنطقي:** الـ Cart بالـ Session (25) ما بيحتاج تسجيل دخول أصلاً — أي زائر عنده سلة. لكن الانتقال لـ Cart بالـ Database (26) هو أول لحظة فعلية بالمتجر يصير فيها "لازم أعرف مين هذا العميل" لأنه لازم أربط الـ Cart بحساب حقيقي. فمنطقياً، مصادقة العميل (Fortify) لازم تُقدَّم بالضبط هون — قبل ما نحتاجها، مش بعدها.

### الترتيب الجديد لمحور 6 (8 محاضرات بدل 7)

1. سلة التسوق: Session (كما هي)
2. **[جديدة] Fortify وSocialite لتسجيل دخول العميل** — تثبيت Fortify للـ Storefront تحديداً (منفصل سياقياً عن Breeze في محور 5)، تسجيل دخول العميل، تسجيل حساب جديد، تسجيل دخول بـ Google (Socialite: Redirect, Callback, ربط بحساب موجود)
3. سلة التسوق: Database (كما هي، الآن مع سياق كامل — نعرف مين المستخدم فعلاً)
4. العناوين والشحن والكوبونات (كما هي)
5. نظام الطلبات (كما هي)
6. إدارة الطلبات في Admin (كما هي)
7. Wishlist وHelper Classes (كما هي)
8. Reviews (كما هي)

---

# 💳 محور 7 — الدفع الإلكتروني

**الحكم: ✅ ممتاز كما هو. لا تعديل مطلوب.**

---

# 🔌 محور 8 — REST API

**الحكم: ✅ ممتاز كما هو. لا تعديل مطلوب.**

موضوع متقدم منطقي بمكانه — بعد اكتمال النظام الأساسي بالكامل (متجر + دفع)، منطقي نفتحه كواجهة API لتطبيق جوال منفصل.

---

# ⚡ محور 9 — Real-time & Notifications

**الحكم: ✅ ممتاز كما هو. لا تعديل مطلوب.**

---

# 🔄 محور 10 — Events & Queues

**الحكم: ✅ ممتاز كما هو. لا تعديل مطلوب.**

---

# 🌍 محور 11 — Localization

**الحكم: ✅ يبقى (أكدت أهميته)، بدون تعديل.**

---

# ⚙️ محور 12 — النظام والأداء والأمان (يستقبل Gates/Policies)

**الحكم: 🔀 هذا هو "المكان الطبيعي" الجديد لـ Gates/Policies، بالإضافة لإعادة تشكيل محتوى Security.**

### لماذا هون بالتحديد منطقي لـ Gates/Policies؟

بهذه المرحلة من الدورة، عند الطالب فعلياً: منتجات، فئات، طلبات، مستخدمين بأدوار (admin/customer)، مراجعات، كوبونات — نظام متجر شبه مكتمل بمشاكل صلاحيات حقيقية ومتعددة تستحق حل دقيق (Policies لكل Model على حدة)، بدل ما تكون Gates/Policies "نظرية مبكرة" بمحور 5 قبل ما يكون عنده شيء يطبّق عليه الصلاحية أصلاً.

### الترتيب الجديد لمحور 12 (3 محاضرات بدل 2)

1. **[منقولة من محور 5] Gates وPolicies بالعمق** — `Gate::define()`, `$this->authorize()`, `@can`/`@cannot`, `php artisan make:policy`, Policy Methods كاملة (viewAny, view, create, update, delete), `before()` للأدمن — مطبقة على كل الـ Models المتراكمة (Product, Order, Review...)
2. Error Handling وCaching (كما هي، بدون تغيير)
3. **Security Basics (معدّلة)** — يبقى فيها: SQL Injection, XSS, Rate Limiting كمواضيع أساسية عامة تحتاج "تجميع أخير". **لكن CSRF وMass Assignment Protection يُحذفان من هنا** لأنهما فعلياً **مفهومان مطبقان عملياً من محاضرات سابقة** (Mass Assignment اتذكر بمحاضرة "Eloquent Basics" و"CRUD Create" بمحور 3 عبر `$fillable`؛ CSRF جزء أصيل من أي Blade Form من محور 4). **الإضافة المقترحة:** بدل تكرارهما هنا كموضوع "جديد"، هذه المحاضرة تصير **مراجعة/تجميع أمني شامل** يذكّر الطالب أين طبّق كل مبدأ أمان سابقاً + يضيف عليه ما هو جديد فعلاً (SQL Injection عبر Query Builder الآمن، XSS عبر `{{ }}` التلقائي بـ Blade، Rate Limiting على Forms/API).

**2FA:** يُذكر هنا كملحق اختياري سريع ضمن محاضرة Gates/Policies (أو يُحذف تماماً من المسار الأساسي) — ليس أساسياً لـ v1 من SouqPro.

---

# 🚀 محور 13 — الإطلاق والنشر

**الحكم: ✅ ممتاز كما هو. لا تعديل مطلوب.**

---

# 🤖 محور 14 — الذكاء الاصطناعي

**الحكم: ✅ ممتاز كما هو، خاتمة منطقية للدورة. لا تعديل مطلوب.**

---

## 📋 ملخص عدد المحاضرات (قبل/بعد)

| المحور | قبل | بعد | الفرق |
|---|---|---|---|
| 1 | 5 | 5 | = (محتوى معاد ترتيبه فقط) |
| 2 | 2 | 2 | = |
| 3 | 7 | 9 | +2 (استقبلت Breeze + Controllers/Routes) |
| 4 | 5 | 3 | **-2 (دمج)** |
| 5 | 5 | 1 | **-4 (تفكيك: Breeze→3، Fortify/Socialite→6، Gates/Policies/2FA→12)** — أو يُدمج بمحور 3 (خيار أ أعلاه)، فيصبح 0 كمحور مستقل |
| 6 | 7 | 8 | +1 (استقبلت Fortify/Socialite) |
| 7-11 | 15 | 15 | = |
| 12 | 2 | 3 | +1 (استقبلت Gates/Policies) |
| 13-14 | 2 | 2 | = |
| **المجموع** | **48** | **48** | **بدون تغيير بالعدد الكلي** — إعادة توزيع فقط |

---

## ⚠️ ملاحظات للـ Agent المنفّذ

- هذا الملف **لا يغيّر عدد المحاضرات الإجمالي (48)** ولا الساعات الإجمالية (~47 ساعة) بشكل جوهري — التعديل الأساسي هو **إعادة توزيع نفس المحتوى** بترتيب أصح منطقياً، مع تخفيف حقيقي واحد فقط (محور 4: دمج HasOne مع HasMany، تخفيف Global Scopes).
- عند التنفيذ الفعلي بـ Notion: حدّث الجدول العلوي (📊 ملخص المحاور) ليعكس أرقام المحاضرات الجديدة بكل محور بعد التوزيع (أرقام المحاضرات 1-48 هتتغير تسلسلياً بسبب الدمج/النقل).
- **لا تحذف أي مفهوم تقني فعلي** المذكور بالمحتوى الأصلي (CSRF, Mass Assignment, 2FA, Global Scopes) — هذه **تُعاد توضيعها أو تُختصر إشارتها**، وليست تُحذف من الدورة كمعرفة، فقط من كونها "محاضرة كاملة مستقلة" حيث لم تعد تستحق ذلك بهذه المرحلة.
- محور 4 هو المحور الوحيد اللي فيه **تخفيف فعلي** (تقليل ساعات) — كل باقي التعديلات هي **إعادة ترتيب/دمج** بدون فقدان محتوى.
- لا تلمس محور 1 — معالج بالكامل في ملف منفصل (`souqpro-module1-revision.md`).